{
  "name": "table-wise-restaurant-ordering-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "db:migrate": "cd server && npx prisma db push",
    "db:seed": "cd server && npm run seed",
    "db:studio": "cd server && npx prisma studio",
    "build": "npm install --prefix client && npm install --prefix server && npm run build --prefix client && npm run build --prefix server"
  }
}
